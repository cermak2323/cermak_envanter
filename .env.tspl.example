# TSPL Barcode Printer Configuration
# Thermal barcode printer'ın IP adresi ve portu

# Yazıcı IP adresi (localhost = USB bağlı, veya yazıcı IP adresi)
TSPL_PRINTER_HOST=localhost

# Yazıcı port numarası (9100 = standard TSPL port)
TSPL_PRINTER_PORT=9100

# TSPL desteğini aktif et
# true = QR kodları TSPL ile yazıcıdan çıktı alacak
# false = sadece PNG dosyası oluşturulacak
TSPL_ENABLED=false
